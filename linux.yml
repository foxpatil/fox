---
 - hosts: linux
   become: true
   gather_facts: false


   tasks:
        - name: create directory
          file:
                  path: /tmp/cdate/
                  state: directory
                 
                 
       
