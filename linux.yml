---
 - hosts: all
   become: true
   gather_facts: false


   tasks:
        - name: create directory
          when: file:
                  path: /tmp/cdate/
                  state: directory
                 
                 
       
